name = "japan-briefing-refresh"
main = "worker.js"
compatibility_date = "2024-01-01"

# Cron trigger: 22:00 UTC = 7am JST
# Fires the workflow even if GitHub skips the cron due to repo inactivity
[triggers]
crons = ["0 22 * * *"]
